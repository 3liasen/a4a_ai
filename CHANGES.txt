## 0.4.0
- Added dedicated admin subpages for the dashboard, clients, and categories while wiring new REST endpoints for each entity.
- Extended the crawler dashboard to capture client assignments, AI prompts, and run-now overrides, updating detail cards, tables, and forms accordingly.
- Delivered a lightweight clients workspace that supports creating, editing, deleting, and running URLs per client from a single screen.
- Introduced category management with unlimited option lists and inline editing tools.
